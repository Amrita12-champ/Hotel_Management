spring:
  application:
    name: UserService

  datasource:
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: a34862@TKRS
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: truemy